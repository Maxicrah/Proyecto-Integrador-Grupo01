<!DOCTYPE html>
<html lang="es">

<head>
        <%- include('partial/head') %>

</head>

<body>

    <%- include('partial/header') %>
        <main class="form-container">

            <form class="form" method="POST" action="/user/register">
                <h2 class="titulo-form-registro">Formulario de Registro</h2>
                <% if (locals.errors) { %>
                    <% errors.forEach(err => { %>
                        <p class="msg-error"><%= err.msg %></p>
                    <% }) %>
                <% } %>
                
                <div class="div-form">
                    <label for="nombre">Nombre de usuario </label>
                    <input type="text" id="nombre" name="username" value="<%= locals.old ? old.username : '' %>">
                </div>
            
                <div class="div-form">
                    <label for="nombre">Nombre</label>
                    <input type="text" id="nombre" name="nombre" value="<%= locals.old ? old.nombre : '' %>">
                </div>
            
                <div class="div-form">
                    <label for="apellido">Apellido</label>
                    <input type="text" id="apellido" name="apellido" value="<%= locals.old ? old.apellido : '' %>">
                </div>
            
                <div class="div-form">
                    <label for="email">Correo Electrónico</label>
                    <input type="email" id="email" name="email" value="<%= locals.old ? old.email : '' %>">
                </div>
            
                <div class="div-form">
                    <label for="reemail">Confirmar Correo Electrónico</label>
                    <input type="email" id="reemail" name="re_email" value="<%= locals.old ? old.re_email : '' %>">
                </div>
            
                <div class="div-form">
                    <label for="password">Contraseña</label>
                    <input type="password" id="password" name="password">
                </div>
            
                <div class="div-form">
                    <label for="repassword">Confirmar Contraseña</label>
                    <input type="password" id="repassword" name="re_password">
                </div>
            
                <div class="div-form-bt">
                    <button type="submit" class="btnPrimary">Registrarse</button>
                    <button type="reset" class="btnPrimary">Cancelar</button>
                </div>
            </form>
            </main>
            

        <%- include('partial/footer') %>
</body>

</html>